---
import { getCollection } from 'astro:content';

const allGalleryItems = await getCollection('gallery');
// Tomamos solo los primeros 4 para el preview del Home
const images = allGalleryItems.slice(0, 4).map(item => ({
  src: item.data.image,
  alt: item.data.title,
  category: item.data.category
}));
---
<section class="gallery" id="gallery">
  <div class="container">
    <div class="gallery-header fade-in">
      <p class="section-label">Portafolio</p>
      <h2 class="section-title">Galería de <em>Proyectos</em></h2>
      <p class="gallery-intro">
        Explora algunas de nuestras creaciones más destacadas. Cada proyecto es diseñado a 
        medida para contar la historia única de nuestros clientes.
      </p>
    </div>

    <div class="gallery-grid">
      {images.map((img, index) => (
        <div class={`gallery-item fade-in d-${(index % 4) + 1}`}>
          <img src={img.src} alt={img.alt} class="gallery-img" loading="lazy" />
          <div class="gallery-overlay">
            <span class="gallery-category">{img.category}</span>
            <h4 class="gallery-item-title">{img.alt}</h4>
          </div>
        </div>
      ))}
    </div>
    
    <div class="gallery-cta fade-in">
      <a href="/galeria" class="btn btn-outline">
        Explorar Portafolio Completo
      </a>
    </div>
  </div>
</section>

<style>
.gallery {
  background: var(--color-bg);
}

.gallery-header {
  text-align: center;
  max-width: 600px;
  margin: 0 auto 4rem;
  display: flex;
  flex-direction: column;
  align-items: center;
}
.gallery-header .section-label {
  justify-content: center;
}
.gallery-header .section-label::before {
  display: none;
}

.gallery-intro {
  color: var(--color-text);
  font-size: var(--size-md);
  margin-top: -0.5rem;
}

.gallery-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
  gap: 1.5rem;
}

.gallery-item {
  position: relative;
  aspect-ratio: 1;
  border-radius: var(--radius-md);
  overflow: hidden;
  cursor: pointer;
}

.gallery-img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.6s cubic-bezier(0.2, 0.8, 0.2, 1);
}

.gallery-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(to top, rgba(43, 40, 38, 0.8) 0%, rgba(43, 40, 38, 0) 50%);
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
  padding: 1.5rem;
  opacity: 0;
  transition: opacity 0.4s ease;
}

.gallery-item:hover .gallery-img {
  transform: scale(1.05);
}
.gallery-item:hover .gallery-overlay {
  opacity: 1;
}

.gallery-category {
  font-size: 0.75rem;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--color-accent);
  font-weight: 700;
  margin-bottom: 0.25rem;
}

.gallery-item-title {
  color: white;
  font-family: var(--font-serif);
  font-size: var(--size-lg);
  font-weight: 400;
}

.gallery-cta {
  margin-top: 4rem;
  text-align: center;
}

@media (max-width: 768px) {
  .gallery-grid {
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
  }
}
</style>
